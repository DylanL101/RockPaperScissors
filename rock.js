const playOption =["ROCK", "PAPER", "SCISSORS"];
function computerPlay(){
    return playOption[Math.floor(Math.random()*playOption.length)]
    
}
    console.log(playOption[Math.floor(Math.random()*playOption.length)]);